version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - curl https://intoli.com/install-google-chrome.sh | bash
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
